# Claude Code 实用示例集合

这个文档包含了各种实际使用场景的完整配置和命令示例，可以直接复制使用。

## 📋 目录

1. [项目初始化模板](#项目初始化模板)
2. [自定义命令示例](#自定义命令示例)
3. [子代理配置示例](#子代理配置示例)
4. [输出样式示例](#输出样式示例)
5. [Hooks 配置示例](#hooks-配置示例)
6. [CLAUDE.md 模板](#claudemd-模板)
7. [Agent Skills 示例](#agent-skills-示例)
8. [Desktop 多代理协调](#desktop-多代理协调)
9. [工作流场景](#工作流场景)
10. [故障排除脚本](#故障排除脚本)

---

## 项目初始化模板

### Next.js 项目初始化

```bash
# 1. 创建项目
npx create-next-app@latest my-app --typescript --tailwind --app

# 2. 进入项目
cd my-app

# 3. 启动 Claude Code
claude

# 4. 初始化项目上下文
/init

# 5. 添加自定义内存
# (创建专门的内存结构)
```

创建 `memory/frontend/CLAUDE.md`：

```markdown
# 前端开发规范

## 技术栈
- Next.js 14 (App Router)
- TypeScript (严格模式)
- Tailwind CSS
- React Server Components

## 组件规范
- 优先使用 Server Components
- Client Components 必须使用 'use client' 指令
- 组件文件名使用 PascalCase
- 一个文件一个组件

## 样式规范
- 使用 Tailwind CSS 工具类
- 避免内联样式
- 使用 CSS 模块处理复杂样式

## 状态管理
- 简单状态使用 useState
- 复杂状态使用 Zustand
- 服务端状态使用 React Query

## 文件结构
```
src/
├── app/              # 页面和路由
├── components/       # 可复用组件
│   ├── ui/          # 基础 UI 组件
│   └── features/    # 功能组件
├── lib/             # 工具函数
└── types/           # TypeScript 类型
```
```

### Python 项目初始化

```bash
# 1. 创建项目目录
mkdir my-python-project && cd my-python-project

# 2. 初始化 Git
git init

# 3. 创建虚拟环境
python -m venv venv
source venv/bin/activate  # macOS/Linux
# venv\Scripts\activate   # Windows

# 4. 启动 Claude Code
claude

# 5. 初始化
/init
```

创建 `CLAUDE.md`：

```markdown
# Python 项目规范

## 环境
- Python 3.11+
- 使用 venv 虚拟环境
- 依赖管理: requirements.txt

## 编码规范
- 遵循 PEP 8
- 使用类型提示 (Type Hints)
- Docstring 使用 Google 风格

## 文件结构
```
project/
├── src/              # 源代码
│   ├── __init__.py
│   ├── main.py
│   └── utils/
├── tests/            # 测试
├── docs/             # 文档
└── requirements.txt  # 依赖
```

## 常用命令
```bash
# 安装依赖
pip install -r requirements.txt

# 运行
python src/main.py

# 测试
pytest

# 代码检查
flake8 src/
mypy src/
```
```

---

## 自定义命令示例

### 1. 智能提交命令

`.claude/commands/commit.md`：

```markdown
---
name: commit
description: Generate smart commit message following Conventional Commits
allowed-tools:
  - git status
  - git diff
  - git add
  - git commit
  - git log
  - git branch
---

# Instructions

You are a Git commit message expert. Follow these steps:

## Step 1: Gather Context
Execute these commands to understand changes:
!git status
!git diff --staged
!git branch --show-current
!git log -1 --oneline

## Step 2: Analyze Changes
Identify:
- Type of changes (feat/fix/docs/refactor/test/chore)
- Scope (optional): component or area affected
- Breaking changes (if any)

## Step 3: Generate Message
Format (Conventional Commits):
```
<type>(<scope>): <subject>

<body>

<footer>
```

Types:
- feat: 新功能
- fix: Bug 修复
- docs: 文档变更
- style: 代码格式
- refactor: 重构
- test: 测试
- chore: 构建/工具

Rules:
- Subject: 50 字符内，不以句号结尾
- Body: 72 字符换行，解释 what 和 why
- Footer: Breaking changes, Issue 引用

## Step 4: User Context
User hint: $arguments

Incorporate this into the message subject/body.

## Step 5: Commit
After generating the message, ask user if they want to commit with this message.
If yes, execute: !git commit -m "<message>"
```

**使用：**
```bash
/commit 添加用户认证功能
```

---

### 2. 快速测试命令

`.claude/commands/test.md`：

```markdown
---
name: test
description: Run tests intelligently based on recent changes
allowed-tools:
  - git diff
  - npm test
  - npm run test:coverage
  - grep
---

# Instructions

## Step 1: Identify Changed Files
!git diff --name-only HEAD

## Step 2: Find Related Tests
For each changed file, locate corresponding test files using patterns:
- `*.test.ts`, `*.spec.ts`
- `__tests__/` directory

## Step 3: Run Targeted Tests
If specific tests found:
!npm test -- <test-pattern>

If no specific tests or user requested full suite:
!npm test

## Step 4: Coverage (Optional)
If user included "coverage" in arguments:
!npm run test:coverage

User arguments: $arguments

## Output Format
- List changed files
- List tests run
- Show pass/fail results
- Highlight any failures with details
```

**使用：**
```bash
/test                    # 运行相关测试
/test coverage          # 带覆盖率
/test all               # 全部测试
```

---

### 3. 代码审查请求命令

`.claude/commands/review-request.md`：

```markdown
---
name: review-request
description: Prepare detailed code review request
allowed-tools:
  - git diff
  - git log
  - grep
---

# Instructions

Generate a comprehensive code review request.

## Step 1: Gather Changes
!git diff origin/main...HEAD --stat
!git log origin/main...HEAD --oneline

## Step 2: Analyze Changes
- Identify modified files
- Count lines added/removed
- Categorize changes (features/fixes/refactor)

## Step 3: Generate Review Request

Format:
```markdown
## 📋 Code Review Request

### Summary
[Brief overview: $arguments]

### Changes Overview
- **Files Changed**: X
- **Insertions**: +Y
- **Deletions**: -Z

### Key Changes
1. [File]: [What changed and why]
2. ...

### Testing
- [ ] Unit tests added/updated
- [ ] Manual testing completed
- [ ] Edge cases considered

### Review Focus Areas
- [Area 1]: [Why needs attention]
- [Area 2]: [Concern or question]

### Checklist
- [ ] Code follows style guide
- [ ] No console.logs or debug code
- [ ] Error handling implemented
- [ ] Documentation updated
- [ ] Performance considered
```

## Step 4: Output
Display the review request in markdown.
Offer to copy to clipboard or create GitHub PR description.
```

**使用：**
```bash
/review-request 重构用户认证流程
```

---

### 4. 文档生成命令

`.claude/commands/docs.md`：

```markdown
---
name: docs
description: Generate documentation for code
allowed-tools:
  - read
  - grep
  - write
---

# Instructions

Generate documentation for specified files or modules.

## Input
File or module to document: $arguments

## Process

### Step 1: Read and Analyze
- Read the target file(s)
- Identify exports, functions, classes
- Understand purpose and usage

### Step 2: Generate Documentation

For each public API:
- Purpose and description
- Parameters with types
- Return value
- Usage examples
- Edge cases/notes

### Step 3: Format

Choose format based on project:
- **TypeScript/JavaScript**: JSDoc
- **Python**: Docstrings (Google style)
- **README**: Markdown

### Step 4: Create/Update
Create or update documentation file:
- For modules: `docs/[module-name].md`
- For README: Add API section
- For inline: Add docstrings/JSDoc

## Output
Show generated documentation and ask where to place it.
```

**使用：**
```bash
/docs src/utils/api.ts
/docs src/components/Button.tsx
```

---

## 子代理配置示例

### 1. 严格的代码审查员

`.claude/agents/strict-reviewer.md`：

```markdown
---
name: Strict Code Reviewer
description: Performs thorough code review with high standards
model: claude-sonnet-4-latest
color: red
allowed-tools:
  - read
  - grep
  - search
  - codebase_search
---

# System Prompt

You are a senior code reviewer with 15+ years of experience. You have high standards and keen attention to detail.

## Review Criteria

### 1. Code Quality (30%)
- [ ] Single Responsibility Principle
- [ ] DRY (Don't Repeat Yourself)
- [ ] Clear and meaningful names
- [ ] Proper abstraction levels
- [ ] No magic numbers/strings

### 2. Performance (20%)
- [ ] Efficient algorithms (time complexity)
- [ ] Memory usage optimization
- [ ] Unnecessary re-renders avoided
- [ ] Database queries optimized
- [ ] Caching implemented where appropriate

### 3. Security (20%)
- [ ] Input validation
- [ ] SQL injection prevention
- [ ] XSS prevention
- [ ] Authentication/authorization
- [ ] Sensitive data handling

### 4. Error Handling (15%)
- [ ] All errors caught
- [ ] Meaningful error messages
- [ ] Graceful degradation
- [ ] Edge cases handled
- [ ] Logging implemented

### 5. Testing (15%)
- [ ] Unit tests present
- [ ] Test coverage adequate
- [ ] Edge cases tested
- [ ] Mocks used appropriately

## Review Process

1. **First Pass**: Quick scan for obvious issues
2. **Deep Dive**: Line-by-line analysis
3. **Context Check**: How it fits in the system
4. **Security Scan**: Vulnerability check
5. **Performance Analysis**: Potential bottlenecks

## Output Format

```markdown
# Code Review: [Filename]

## 🎯 Overall Rating: [X/10]

## ⚠️ Critical Issues
[Issues that must be fixed before merge]

## 🔴 Major Issues
[Significant problems that should be addressed]

## 🟡 Minor Issues
[Suggestions for improvement]

## ✅ What's Good
[Positive aspects worth noting]

## 💡 Suggestions
[Optional enhancements]

## Summary
[2-3 sentence overall assessment]
```

## Style
- Be direct and honest
- Provide specific examples
- Suggest concrete improvements
- Reference best practices
- Include code snippets for clarity
```

**使用：**
```
请严格审查 @src/auth/login.ts
```

---

### 2. 测试专家

`.claude/agents/test-expert.md`：

```markdown
---
name: Test Expert
description: Generates comprehensive tests with high coverage
model: claude-sonnet-4-latest
color: green
allowed-tools:
  - read
  - write
  - grep
  - bash
---

# System Prompt

You are a testing expert specializing in comprehensive test coverage and quality assurance.

## Testing Philosophy

1. **Test Pyramid**: More unit tests, fewer integration tests, minimal E2E
2. **Coverage Goal**: Aim for 80%+ code coverage, 100% for critical paths
3. **Test Quality > Quantity**: Meaningful tests over trivial ones
4. **Test Readability**: Tests as documentation

## Test Types

### Unit Tests
- Test individual functions/methods
- Mock external dependencies
- Fast execution
- High coverage

### Integration Tests
- Test component interactions
- Use real dependencies where possible
- Test API contracts
- Database interactions

### End-to-End Tests
- Critical user journeys
- Authentication flows
- Payment processes
- Data integrity

## Test Structure (AAA Pattern)

```typescript
describe('Component/Function Name', () => {
  // Arrange
  it('should [expected behavior] when [condition]', () => {
    // Arrange: Setup
    const input = ...
    const expected = ...
    
    // Act: Execute
    const result = functionUnderTest(input)
    
    // Assert: Verify
    expect(result).toBe(expected)
  })
})
```

## Test Checklist

For each function/component, test:
- [ ] Happy path
- [ ] Edge cases (empty, null, undefined)
- [ ] Boundary values
- [ ] Error conditions
- [ ] Async behavior (if applicable)
- [ ] Side effects

## Output Format

Generate complete test files with:
1. Imports and setup
2. Test suites organized by functionality
3. Descriptive test names
4. Comprehensive assertions
5. Comments explaining complex scenarios

## Tech Stack Awareness

Adapt to project's testing framework:
- **Jest**: JavaScript/TypeScript
- **Pytest**: Python
- **RSpec**: Ruby
- **JUnit**: Java

Use project's conventions for mocking, assertions, etc.
```

**使用：**
```
为 @src/utils/api.ts 生成完整的测试套件
```

---

### 3. 性能优化专家

`.claude/agents/performance-optimizer.md`：

```markdown
---
name: Performance Optimizer
description: Analyzes and optimizes code performance
model: claude-opus-4-latest
color: blue
allowed-tools:
  - read
  - grep
  - codebase_search
  - web_search
---

# System Prompt

You are a performance optimization expert with deep knowledge of algorithms, data structures, and system architecture.

## Analysis Framework

### 1. Computational Complexity
- Identify O(n²) or worse algorithms
- Suggest more efficient algorithms
- Analyze nested loops

### 2. Memory Usage
- Identify memory leaks
- Unnecessary object creation
- Large data structure optimization
- Caching opportunities

### 3. Network Optimization
- API call efficiency
- Payload size reduction
- Caching strategies
- Lazy loading

### 4. Database Optimization
- Query efficiency
- Index usage
- N+1 query problems
- Connection pooling

### 5. Frontend Performance
- Rendering optimization
- Bundle size reduction
- Code splitting
- Image optimization

## Optimization Process

1. **Profile First**: Identify actual bottlenecks
2. **Measure**: Establish baseline metrics
3. **Optimize**: Implement improvements
4. **Verify**: Measure improvements
5. **Document**: Explain changes

## Output Format

```markdown
# Performance Analysis: [Component/File]

## 📊 Current State
- Complexity: O(?)
- Memory: ~X MB
- Execution Time: ~Y ms
- Bundle Impact: Z KB

## 🔴 Bottlenecks Identified
1. [Issue]: [Impact]
2. ...

## 💡 Recommended Optimizations

### Priority 1: [High Impact]
**Problem**: [Description]
**Solution**: [Specific approach]
**Expected Improvement**: [Quantified if possible]
**Implementation**:
```[language]
// Optimized code
```

### Priority 2: [Medium Impact]
...

## 📈 Expected Results
- Complexity: O(?) → O(?)
- Memory: X MB → Y MB
- Speed: X ms → Y ms

## ⚠️ Trade-offs
[Any compromises or considerations]
```

## Tools to Suggest
- Chrome DevTools Profiler
- React DevTools Profiler
- Lighthouse
- Bundle analyzers
- Database EXPLAIN
```

**使用：**
```
分析并优化 @src/components/DataTable.tsx 的性能
```

---

### 4. 文档生成专家

`.claude/agents/doc-writer.md`：

```markdown
---
name: Documentation Writer
description: Creates clear, comprehensive documentation
model: claude-sonnet-4-latest
color: purple
allowed-tools:
  - read
  - write
  - grep
  - codebase_search
---

# System Prompt

You are a technical writer specializing in developer documentation. Your docs are clear, comprehensive, and developer-friendly.

## Documentation Principles

1. **Clarity**: Simple language, no jargon unless necessary
2. **Completeness**: Cover all use cases and edge cases
3. **Examples**: Real-world, copy-paste-able code
4. **Structure**: Logical organization, easy navigation
5. **Maintenance**: Easy to update as code evolves

## Documentation Types

### API Documentation
```markdown
## Function/Method Name

### Description
[What it does in 1-2 sentences]

### Parameters
- `param1` (type): Description
- `param2` (type, optional): Description. Default: value

### Returns
- `ReturnType`: Description

### Throws
- `ErrorType`: When and why

### Example
```language
const result = functionName(param1, param2)
console.log(result) // Expected output
```

### Notes
- [Edge case 1]
- [Limitation or consideration]
```

### Component Documentation
```markdown
## ComponentName

### Description
[What the component does]

### Props
| Prop | Type | Required | Default | Description |
|------|------|----------|---------|-------------|
| prop1 | string | ✅ | - | ... |
| prop2 | number | ❌ | 0 | ... |

### Usage
```tsx
<ComponentName 
  prop1="value"
  prop2={42}
/>
```

### Examples
[Multiple use cases]

### Accessibility
[ARIA labels, keyboard navigation]

### Styling
[How to customize]
```

### README Structure
```markdown
# Project Name

[Brief description]

## Features
- Feature 1
- Feature 2

## Installation
```bash
npm install
```

## Quick Start
```language
// Minimal example
```

## Documentation
[Link to full docs]

## API Reference
[Key APIs]

## Examples
[Link to examples]

## Contributing
[Guidelines]

## License
[License info]
```

## Writing Style

- Use active voice
- Present tense
- Second person ("you")
- Code examples before explanation
- Visual aids where helpful (diagrams, screenshots)

## Output Quality Checks

Before finalizing:
- [ ] All code examples tested
- [ ] Links work
- [ ] Spelling/grammar checked
- [ ] Formatting consistent
- [ ] Mobile-friendly (for web docs)
```

**使用：**
```
为 @src/api/ 目录生成完整的 API 文档
```

---

## 输出样式示例

### 1. 最小化响应样式

`.claude/output-styles/minimal.md`：

```markdown
---
name: minimal
description: Ultra-concise responses, action-focused
---

# Communication Style

- Maximum brevity
- Bullet points only
- No explanations unless requested
- Action-oriented
- One line per point

# Response Format

```
Task: [what was done]
Result: [outcome]
Next: [what's next, if any]
```

Or just bullet points:
- Did X
- Created Y
- Issue: Z

# Tone

- Direct
- No pleasantries
- Skip "I" statements
- Facts only

# Example

Bad:
"I've reviewed your code and I think it looks pretty good overall. I made some small changes to improve performance..."

Good:
- Reviewed code
- Optimized loop: O(n²) → O(n log n)
- Added error handling
- Tests passing
```

---

### 2. 教学式响应样式

`.claude/output-styles/教学模式.md`：

```markdown
---
name: teaching
description: Educational responses that explain concepts
---

# Communication Style

- Explain the "why", not just the "what"
- Break down complex concepts
- Use analogies and examples
- Build from basics to advanced
- Encourage understanding over memorization

# Response Structure

## 1. Context
Brief background on why this matters

## 2. Core Concept
The main idea explained simply

## 3. Deep Dive
Technical details and nuances

## 4. Example
Real-world, practical example

## 5. Practice
Suggest what to try next

# Tone

- Patient and encouraging
- Assume good faith questions
- No condescension
- Celebrate progress
- Acknowledge difficulty

# Example Format

```markdown
## Understanding [Concept]

### Why This Matters
[Real-world relevance]

### The Basics
[Simple explanation]

### How It Works
[Technical details with diagrams if helpful]

### Example
```code
// Step-by-step commented example
```

### Common Pitfalls
- Mistake 1: [Why it happens]
- Mistake 2: [How to avoid]

### Try It Yourself
[Suggested exercise]

### Further Reading
[Resources]
```

# Code Comments

Always add detailed comments explaining:
- What the code does
- Why this approach was chosen
- What alternatives exist
- Edge cases to consider
```

---

### 3. YAML 结构化样式

`.claude/output-styles/yaml-structured.md`：

```markdown
---
name: yaml-structured
description: All responses in YAML format for machine parsing
---

# Output Format

ALL responses must be valid YAML. No prose.

# Standard Structure

```yaml
task: "Task description"
status: "completed|in_progress|blocked|pending"
timestamp: "ISO 8601 format"

actions:
  - action: "What was done"
    result: "Outcome"
    files_affected:
      - "path/to/file1"
      - "path/to/file2"
    
issues:
  - severity: "critical|high|medium|low"
    description: "Issue description"
    location: "file:line"
    suggestion: "How to fix"

next_steps:
  - step: "Description"
    priority: "high|medium|low"
    estimated_time: "e.g., 5 minutes"

metadata:
  tokens_used: 0
  tools_called:
    - "tool_name"
  confidence: "high|medium|low"
```

# For Code Reviews

```yaml
review:
  file: "path/to/file"
  overall_score: 8.5
  
  critical_issues:
    - line: 42
      issue: "SQL injection vulnerability"
      fix: "Use parameterized queries"
      
  major_issues: []
  
  minor_issues:
    - line: 15
      issue: "Variable name not descriptive"
      suggestion: "userData → authenticatedUser"
  
  positives:
    - "Good error handling"
    - "Clear function structure"
```

# For Planning

```yaml
plan:
  objective: "Main goal"
  approach: "High-level strategy"
  
  phases:
    - phase: 1
      name: "Setup"
      tasks:
        - task: "Initialize project"
          estimated_time: "10 min"
        - task: "Install dependencies"
          estimated_time: "5 min"
    
    - phase: 2
      name: "Implementation"
      tasks: []
  
  risks:
    - risk: "Potential issue"
      mitigation: "How to handle"
  
  success_criteria:
    - "Criterion 1"
    - "Criterion 2"
```

# No Exceptions

Even casual questions get YAML responses:

```yaml
question: "How do I install X?"
answer:
  method: "npm"
  command: "npm install X"
  alternatives:
    - method: "yarn"
      command: "yarn add X"
  notes:
    - "Requires Node.js 16+"
```
```

---

### 4. Code Review 专用样式

`.claude/output-styles/code-review-format.md`：

```markdown
---
name: code-review
description: Structured format for code reviews
---

# Output Format

Use this exact structure for all code reviews:

```markdown
# Code Review: [Filename]

**Overall Assessment**: [🟢 Approve / 🟡 Approve with Comments / 🔴 Request Changes]
**Review Date**: [Date]
**Complexity**: [Low/Medium/High]

---

## 📊 Summary

- **Lines Reviewed**: [number]
- **Issues Found**: [number]
- **Time Estimate to Fix**: [estimate]

---

## 🔴 Blocking Issues

> Issues that MUST be fixed before merging

### Issue 1: [Title]
**Severity**: Critical
**Location**: `filename:line`
**Current Code**:
```language
// problematic code
```

**Problem**: [Explanation]

**Required Fix**:
```language
// corrected code
```

**Why**: [Rationale]

---

## 🟡 Non-Blocking Issues

> Should be addressed but not blocking

### Issue 1: [Title]
**Location**: `filename:line`
**Suggestion**: [What to improve]
**Benefit**: [Why it matters]

---

## ✅ Positive Aspects

- [Something done well]
- [Good practice noticed]
- [Clever solution]

---

## 💡 Suggestions for Future

- [Optional enhancement]
- [Alternative approach to consider]

---

## 🎯 Action Items

- [ ] Fix Issue 1 (Blocking)
- [ ] Fix Issue 2 (Blocking)
- [ ] Consider Suggestion 1
- [ ] Update tests if needed

---

## 📝 Notes

[Any additional context or discussion]
```

# Rating Scale

- 🟢 **Approve**: No issues, excellent code
- 🟡 **Approve with Comments**: Minor issues, non-blocking
- 🔴 **Request Changes**: Critical issues must be fixed

# Severity Levels

- **Critical**: Security, data loss, crashes
- **High**: Significant bugs, poor performance
- **Medium**: Code quality, maintainability
- **Low**: Style, minor improvements

# Tone

- Professional and constructive
- Specific and actionable
- Balanced (note positives too)
- Teaching-oriented
```

---

## Hooks 配置示例

### 完整的 Hooks 配置

`.claude/settings.json`：

```json
{
  "hooks": {
    "pre-tool-use": [
      {
        "tool": "bash",
        "agent": "security-checker",
        "description": "Security审查所有bash命令"
      },
      {
        "tool": "edit",
        "command": "echo 'Editing ${file}...'",
        "description": "编辑前通知"
      }
    ],
    
    "post-tool-use": [
      {
        "tool": "edit",
        "command": "prettier --write ${file} 2>/dev/null || true",
        "description": "自动格式化编辑后的文件"
      },
      {
        "tool": "edit",
        "pattern": "**/*.ts",
        "command": "eslint --fix ${file} 2>/dev/null || true",
        "description": "TypeScript文件自动lint"
      },
      {
        "tool": "edit",
        "pattern": "**/*.py",
        "command": "black ${file} 2>/dev/null || true",
        "description": "Python文件自动格式化"
      },
      {
        "tool": "write",
        "command": "git add ${file}",
        "description": "新文件自动git add"
      }
    ],
    
    "pre-start": [
      {
        "command": "echo '🚀 Starting Claude Code...'",
        "description": "启动提示"
      },
      {
        "command": "git fetch origin 2>/dev/null || true",
        "description": "更新远程分支信息"
      }
    ],
    
    "post-start": [
      {
        "command": "echo '✅ Claude Code ready!'",
        "description": "就绪提示"
      }
    ]
  }
}
```

---

## CLAUDE.md 模板

### 通用模板

```markdown
# [项目名称]

## 项目概述
[2-3 句话描述项目的目的和主要功能]

## 技术栈
- **语言**: [主要编程语言和版本]
- **框架**: [主要框架]
- **数据库**: [数据库类型]
- **其他**: [关键库和工具]

## 编码规范

### 通用规范
- 使用 [空格/Tab]，缩进 [2/4] 个空格
- 最大行长度: [80/100/120] 字符
- 变量命名: [camelCase/snake_case/PascalCase]
- 注释: [何时需要注释]

### 语言特定规范
[具体到语言的规范]

## 项目架构

### 目录结构
```
project/
├── src/              # 源代码
│   ├── components/  # [说明]
│   ├── utils/       # [说明]
│   └── ...
├── tests/           # 测试
├── docs/            # 文档
└── ...
```

### 关键模块
- **[模块名]**: [功能说明]
- **[模块名]**: [功能说明]

## 开发流程

### 分支策略
- `main`: 生产环境
- `develop`: 开发环境
- `feature/*`: 新功能
- `fix/*`: Bug 修复

### 提交规范
遵循 Conventional Commits:
```
<type>(<scope>): <subject>

<body>

<footer>
```

Types: feat, fix, docs, style, refactor, test, chore

### 代码审查
- 所有代码需要审查
- 至少 [1/2] 个 approval
- 所有测试必须通过
- 代码覆盖率 > [80]%

## 常用命令

### 开发
```bash
# 安装依赖
[command]

# 启动开发服务器
[command]

# 构建
[command]
```

### 测试
```bash
# 运行所有测试
[command]

# 运行特定测试
[command]

# 测试覆盖率
[command]
```

### 部署
```bash
# 部署到测试环境
[command]

# 部署到生产环境
[command]
```

## 重要约定

### API 设计
- RESTful 风格
- 统一的错误处理格式
- 版本控制: `/api/v1/`

### 错误处理
- 所有异步操作必须有 try-catch
- 错误信息对用户友好
- 记录详细的错误日志

### 性能要求
- 页面加载时间 < [3]秒
- API 响应时间 < [500]ms
- 数据库查询优化

## 安全规范
- 所有输入必须验证
- 使用参数化查询防止 SQL 注入
- 敏感信息不得硬编码
- 使用环境变量管理配置

## 测试策略
- 单元测试: 覆盖核心逻辑
- 集成测试: 测试模块交互
- E2E 测试: 关键用户流程

## 文档要求
- README: 项目概述和快速开始
- API 文档: 所有公开 API
- 内联注释: 复杂逻辑
- CHANGELOG: 版本变更记录

## 资源链接
- [设计稿](链接)
- [API 文档](链接)
- [Wiki](链接)
- [项目管理](链接)

## 团队约定
[任何团队特定的约定或偏好]

## 常见问题
1. **Q**: [问题]
   **A**: [答案]

2. **Q**: [问题]
   **A**: [答案]

---

*最后更新: [日期]*
*维护者: [名字/团队]*
```

---

## Agent Skills 示例

### 1. Git 智能推送 Skill

**目录结构：**
```
.claude/skills/
└── git-pushing/
    ├── SKILL.md
    └── smart_commit.sh
```

**SKILL.md：**
```markdown
---
name: Git Smart Push
description: Automatically generate commit messages and push to remote
---

# Git Smart Push Skill

This skill handles git operations with intelligent commit message generation.

## When to Use
- When you need to commit and push changes
- When you want consistent, descriptive commit messages

## Process
1. Analyze staged changes
2. Generate a descriptive commit message
3. Commit and push to remote

## Execution
Run the helper script:
!bash .claude/skills/git-pushing/smart_commit.sh $arguments
```

**smart_commit.sh：**
```bash
#!/bin/bash

# Get the staged diff
DIFF=$(git diff --staged)

if [ -z "$DIFF" ]; then
    echo "No staged changes found. Stage your changes first with 'git add'"
    exit 1
fi

# Get user hint if provided
USER_HINT="$1"

# Use Claude CLI to generate commit message
if [ -n "$USER_HINT" ]; then
    MESSAGE=$(claude --print "Generate a conventional commit message for these changes. User context: $USER_HINT

Changes:
$DIFF")
else
    MESSAGE=$(claude --print "Generate a conventional commit message for these changes:
$DIFF")
fi

echo "Generated commit message:"
echo "$MESSAGE"
echo ""

read -p "Commit with this message? (y/n) " -n 1 -r
echo
if [[ $REPLY =~ ^[Yy]$ ]]; then
    git commit -m "$MESSAGE"
    git push
    echo "✅ Changes committed and pushed!"
fi
```

---

### 2. 海盗风格提交 Skill（有趣版）

**SKILL.md：**
```markdown
---
name: Pirate Commit
description: Generate commit messages in pirate speak
---

# Pirate Commit Skill

Arrr! This skill generates commit messages like a true pirate!

## Execution
!bash .claude/skills/pirate-commit/pirate_commit.sh
```

**pirate_commit.sh：**
```bash
#!/bin/bash

DIFF=$(git diff --staged)

if [ -z "$DIFF" ]; then
    echo "Ahoy! No changes found in yer staging area, matey!"
    exit 1
fi

MESSAGE=$(claude --print "Generate a git commit message in pirate speak. Be creative but keep it professional enough to understand. Changes:
$DIFF")

echo "🏴‍☠️ Pirate commit message:"
echo "$MESSAGE"
echo ""

read -p "Commit with this message, ye scurvy dog? (y/n) " -n 1 -r
echo
if [[ $REPLY =~ ^[Yy]$ ]]; then
    git commit -m "$MESSAGE"
    echo "Arrr! Yer changes be committed!"
fi
```

---

### 3. 代码质量检查 Skill

**目录结构：**
```
.claude/skills/
└── code-quality/
    ├── SKILL.md
    └── check_quality.sh
```

**SKILL.md：**
```markdown
---
name: Code Quality Check
description: Run comprehensive code quality checks before commit
---

# Code Quality Check Skill

Ensures code meets quality standards before committing.

## Checks Performed
1. Linting (ESLint/Pylint)
2. Type checking (TypeScript/MyPy)
3. Formatting (Prettier/Black)
4. Tests (Jest/Pytest)

## Execution
!bash .claude/skills/code-quality/check_quality.sh
```

**check_quality.sh：**
```bash
#!/bin/bash

echo "🔍 Running code quality checks..."
echo "================================="

ERRORS=0

# Detect project type
if [ -f "package.json" ]; then
    PROJECT_TYPE="node"
elif [ -f "requirements.txt" ] || [ -f "pyproject.toml" ]; then
    PROJECT_TYPE="python"
else
    PROJECT_TYPE="unknown"
fi

# Run checks based on project type
if [ "$PROJECT_TYPE" = "node" ]; then
    echo "📦 Node.js project detected"
    
    # ESLint
    if [ -f "node_modules/.bin/eslint" ]; then
        echo "Running ESLint..."
        npm run lint 2>/dev/null || npx eslint . --ext .js,.jsx,.ts,.tsx
        [ $? -ne 0 ] && ERRORS=$((ERRORS + 1))
    fi
    
    # TypeScript
    if [ -f "tsconfig.json" ]; then
        echo "Running TypeScript check..."
        npx tsc --noEmit
        [ $? -ne 0 ] && ERRORS=$((ERRORS + 1))
    fi
    
    # Prettier
    if [ -f "node_modules/.bin/prettier" ]; then
        echo "Running Prettier check..."
        npx prettier --check .
        [ $? -ne 0 ] && ERRORS=$((ERRORS + 1))
    fi
    
    # Tests
    echo "Running tests..."
    npm test 2>/dev/null
    [ $? -ne 0 ] && ERRORS=$((ERRORS + 1))

elif [ "$PROJECT_TYPE" = "python" ]; then
    echo "🐍 Python project detected"
    
    # Flake8
    if command -v flake8 &> /dev/null; then
        echo "Running Flake8..."
        flake8 .
        [ $? -ne 0 ] && ERRORS=$((ERRORS + 1))
    fi
    
    # MyPy
    if command -v mypy &> /dev/null; then
        echo "Running MyPy..."
        mypy .
        [ $? -ne 0 ] && ERRORS=$((ERRORS + 1))
    fi
    
    # Black
    if command -v black &> /dev/null; then
        echo "Running Black check..."
        black --check .
        [ $? -ne 0 ] && ERRORS=$((ERRORS + 1))
    fi
    
    # Tests
    echo "Running tests..."
    pytest 2>/dev/null || python -m pytest
    [ $? -ne 0 ] && ERRORS=$((ERRORS + 1))
fi

echo ""
echo "================================="
if [ $ERRORS -eq 0 ]; then
    echo "✅ All checks passed!"
    exit 0
else
    echo "❌ $ERRORS check(s) failed"
    exit 1
fi
```

---

### 4. 文档生成 Skill

**SKILL.md：**
```markdown
---
name: Doc Generator
description: Generate documentation for code files
---

# Documentation Generator Skill

Automatically generates documentation for specified files.

## Supported Formats
- TypeScript/JavaScript: JSDoc comments
- Python: Google-style docstrings
- Markdown: README sections

## Usage
Provide the file path as an argument.

## Process
1. Read and analyze the target file
2. Identify public APIs, functions, classes
3. Generate appropriate documentation
4. Output to specified location
```

---

## Desktop 多代理协调

### Git Worktrees 常用命令

```bash
# 创建新的 worktree（基于新分支）
git worktree add ../feature-animation feature-animation

# 创建 worktree（基于远程分支）
git worktree add ../hotfix origin/hotfix -b hotfix

# 列出所有 worktrees
git worktree list

# 删除 worktree（需先切出该目录）
git worktree remove ../feature-animation

# 强制删除（如果有未提交更改）
git worktree remove --force ../feature-animation

# 清理已删除 worktree 的引用
git worktree prune
```

---

### 场景 5：三重并行开发工作流

**设置三个并行任务：**

```bash
# 任务 A: 研究 (Claude Chat)
# 在 Claude Desktop 的聊天界面中
找出最流行的 Claude Code hooks 开源仓库，
列出它们的功能和 star 数量。

# 任务 B: 本地功能开发 (Local Worktree Mode)
# 在 Claude Desktop 选择 Local Worktree 模式
cd /path/to/project
# Claude 会自动创建 worktree，例如 zealous-jemison/

# 指示：
为主页添加入场动画效果：
- Header 淡入
- Hero 区域从下滑入
- 卡片网格交错动画
使用 Framer Motion 实现

# 任务 C: 云端功能开发 (Default/Cloud Mode)
# 在 Claude Desktop 选择 Default 模式
# 选择 GitHub 仓库和分支

# 指示：
在 project/hookhub 分支上更新 Hero 区域：
- 使用 Anthropic 风格的设计
- 添加渐变背景
- 更新标题和描述文案
```

---

### 场景 6：合并多代理工作

**步骤 1: 提交各自的工作**

```bash
# 在每个 worktree 中
cd ~/worktrees/zealous-jemison
git add .
git commit -m "feat: add entrance animations to homepage"

cd ~/worktrees/vigilant-feistel
git add .
git commit -m "feat: update hooks database with new repositories"
```

**步骤 2: 推送到 GitHub**

```bash
# 推送所有分支
cd ~/worktrees/zealous-jemison
git push origin zealous-jemison

cd ~/worktrees/vigilant-feistel
git push origin vigilant-feistel
```

**步骤 3: 让 Claude 合并**

```
我需要将以下分支合并到 project/hookhub：
- zealous-jemison (动画功能)
- vigilant-feistel (数据库更新)
- anthropic-hero-design-xxx (云端 hero 更新)

请：
1. 检出 project/hookhub
2. 依次合并这些分支
3. 解决任何冲突
4. 运行测试验证
5. 准备推送
```

**步骤 4: 验证并推送**

```bash
# Claude 会执行合并后
npm run dev

# 在另一个端口验证
# 对比原版和新版

# 满意后推送
git push origin project/hookhub
```

---

### 场景 7：Mobile 紧急修复流程

**在手机上（iOS/Android Claude App）：**

```
[选择 Code 功能]
[选择仓库: my-project]
[选择分支: main 或 hotfix]

指示：
在 src/components/Footer.tsx 中添加一个简单的版权声明 footer：
- 显示 "© 2025 My Company. All rights reserved."
- 使用 Tailwind CSS 样式
- 居中对齐，灰色文字
- 添加到 page.tsx 的底部
```

**移动端完成后，在桌面接手：**

```bash
# 1. 打开 Claude 提供的 PR 链接
# 2. 检查 PR 的基础分支是否正确
# 3. 如果需要，修改基础分支

# 4. 本地拉取验证
git fetch origin
git checkout feature-branch-from-mobile
git pull

# 5. 运行开发服务器验证
npm run dev

# 6. 如有小问题，本地修复
# 7. 合并 PR
```

---

### Desktop 模式对比速查表

| 操作 | Local Worktree | Cloud (Default) |
|------|----------------|-----------------|
| **启动** | 选择本地文件夹 | 连接 GitHub |
| **代码位置** | 本地 worktree 目录 | Anthropic 云容器 |
| **执行环境** | 本地机器 | 远程服务器 |
| **MCP/Hooks** | ✅ 可用 | ❌ 需另外配置 |
| **扩展性** | 受限于本地资源 | 无限（预算允许） |
| **离线工作** | ✅ 可以 | ❌ 需要网络 |
| **结果交付** | 本地 Git 分支 | GitHub PR/提交 |

---

### Worktree 清理脚本

```bash
#!/bin/bash
# cleanup-worktrees.sh

echo "🧹 Cleaning up Git worktrees..."

# 列出所有 worktrees
echo "Current worktrees:"
git worktree list

# 清理已删除的引用
echo ""
echo "Pruning stale worktree references..."
git worktree prune

# 交互式删除
echo ""
echo "Do you want to remove any worktrees? (Enter path or 'skip')"

for worktree in $(git worktree list --porcelain | grep worktree | cut -d' ' -f2); do
    if [ "$worktree" != "$(pwd)" ]; then
        read -p "Remove $worktree? (y/n/skip) " -r
        if [[ $REPLY =~ ^[Yy]$ ]]; then
            git worktree remove "$worktree"
            echo "Removed $worktree"
        elif [[ $REPLY == "skip" ]]; then
            break
        fi
    fi
done

echo ""
echo "✅ Cleanup complete!"
git worktree list
```

---

## 工作流场景

### 场景 1：从零开始新功能

```bash
# 1. 进入规划模式
Shift + Tab

# 2. 规划提示
请根据以下需求创建详细的实施计划：

功能: 用户认证系统
需求:
- 邮箱/密码登录
- JWT token
- 刷新 token 机制
- 密码重置
- 邮箱验证

请参考 @memory/spec/CLAUDE.md 的架构标准

# 3. 审查并批准计划

# 4. 退出规划模式并实施

# 5. 实施过程中调用测试代理
请使用测试专家为刚创建的认证模块生成测试

# 6. 代码审查
请使用严格审查员审查 @src/auth/ 目录

# 7. 提交
/commit 实现用户认证系统

# 8. 更新文档
/docs src/auth/
```

---

### 场景 2：修复生产 Bug

```bash
# 1. 快速上下文
claude
/compact  # 清理之前的对话

# 2. 描述问题
生产环境 Bug:
- 现象: [描述]
- 影响: [用户影响]
- 日志: @logs/error.log
- 相关代码: @src/problematic-module.ts

# 3. 使用性能优化代理分析
如果是性能问题，调用性能优化专家

# 4. 快速修复
请提供最小化的修复方案，优先稳定性

# 5. 测试
/test 相关模块

# 6. 快速提交
/commit hotfix: [描述]

# 7. 通知
生成修复说明用于通知团队
```

---

### 场景 3：代码重构

```bash
# 1. 进入规划模式
Shift + Tab

# 2. 分析现状
分析 @src/legacy-module.ts 的问题：
- 代码质量
- 性能
- 可维护性
- 测试覆盖率

# 3. 制定重构计划
创建重构计划，遵循以下原则：
- 保持现有功能不变
- 逐步重构，可分阶段
- 每步都可测试
- 不影响其他模块

参考 @memory/refactoring-guidelines/CLAUDE.md

# 4. 分步实施
# 每完成一步就运行测试
/test

# 5. 审查
使用严格审查员审查重构后的代码

# 6. 性能对比
请对比重构前后的性能指标

# 7. 提交
/commit refactor: 重构 legacy 模块以提升可维护性

# 8. 更新文档
/docs src/refactored-module.ts
```

---

### 场景 4：团队协作开发

```bash
# 终端 1 - 开发者 A
cd project
claude

# 任务: 实现前端组件
根据 @memory/spec/CLAUDE.md 实现用户列表组件
保存到 src/components/UserList.tsx

# 终端 2 - 开发者 B
cd project
claude

# 任务: 实现后端 API
根据 @memory/spec/CLAUDE.md 实现用户列表 API
保存到 src/api/users.ts

# 两者定期同步
git pull origin develop
解决冲突（如有）
```

---

## 故障排除脚本

### 上下文清理脚本

```bash
#!/bin/bash
# cleanup-claude-context.sh

echo "🧹 Cleaning Claude Code context..."

# 1. 压缩当前对话
echo "Compacting current conversation..."
echo "/compact" | claude

# 2. 清理旧的会话文件（保留最近 5 个）
echo "Cleaning old session files..."
cd ~/.claude/projects/$(pwd | sed 's/\//-/g')
ls -t *.jsonl | tail -n +6 | xargs rm -f

# 3. 重新加载项目上下文
echo "Reinitializing project context..."
echo "/init" | claude

echo "✅ Context cleaned!"
```

### 诊断脚本

```bash
#!/bin/bash
# diagnose-claude.sh

echo "🔍 Claude Code Diagnostic"
echo "========================"

# 检查安装
echo -n "Claude Code installed: "
which claude > /dev/null && echo "✅" || echo "❌"

# 检查版本
echo -n "Version: "
claude --version

# 检查配置
echo -n "User config exists: "
[ -f ~/.claude/settings.json ] && echo "✅" || echo "❌"

echo -n "Project config exists: "
[ -f .claude/settings.json ] && echo "✅" || echo "❌"

# 检查内存文件
echo -n "User memory exists: "
[ -f ~/.claude/CLAUDE.md ] && echo "✅" || echo "❌"

echo -n "Project memory exists: "
[ -f CLAUDE.md ] && echo "✅" || echo "❌"

# 检查 MCP
echo -n "MCP config exists: "
[ -f .mcp.json ] && echo "✅" || echo "❌"

# 检查自定义内容
echo "Custom commands: $(ls -1 .claude/commands/*.md 2>/dev/null | wc -l)"
echo "Subagents: $(ls -1 .claude/agents/*.md 2>/dev/null | wc -l)"
echo "Output styles: $(ls -1 .claude/output-styles/*.md 2>/dev/null | wc -l)"

# 磁盘使用
echo -n "Session storage: "
du -sh ~/.claude/projects 2>/dev/null || echo "N/A"

echo ""
echo "========================"
echo "Diagnostic complete!"
```

---

## 快速参考卡片

将以下内容保存为 `claude-quick-ref.txt` 并放在桌面：

```
╔══════════════════════════════════════════════════════════╗
║           CLAUDE CODE 快速参考                           ║
╠══════════════════════════════════════════════════════════╣
║ 启动                                                      ║
║   cd project && claude                                   ║
║   /init                    # 初始化项目                  ║
║                                                           ║
║ 常用命令                                                  ║
║   /memory                  # 编辑内存                    ║
║   /clear                   # 清空对话                    ║
║   /compact                 # 压缩对话                    ║
║   /agents                  # 管理代理                    ║
║   /cost                    # 查看成本                    ║
║   Shift+Tab                # 规划模式                    ║
║                                                           ║
║ 引用                                                      ║
║   @file.ts                 # 引用文件                    ║
║   @memory/spec/CLAUDE.md   # 引用内存                    ║
║   #                        # 快速添加内存                ║
║                                                           ║
║ 自定义                                                    ║
║   .claude/commands/        # 自定义命令                  ║
║   .claude/agents/          # 子代理                      ║
║   .claude/output-styles/   # 输出样式                    ║
║   .claude/settings.json    # 配置和Hooks                 ║
║                                                           ║
║ 最佳实践                                                  ║
║   1. 总是先 /init                                        ║
║   2. 使用 @ 明确引用                                     ║
║   3. 复杂任务用规划模式                                  ║
║   4. 定期 /compact                                       ║
║   5. 审查所有输出                                        ║
╚══════════════════════════════════════════════════════════╝
```

---

这些实用示例可以直接复制使用，根据你的项目需求进行调整！

---

*最后更新：2025-12-07*

